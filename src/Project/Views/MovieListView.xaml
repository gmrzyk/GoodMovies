<UserControl x:Class="Project.Views.MovieListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#252526" Foreground="White">
    <Grid>
        <StackPanel Margin="20">
            <ListView ItemsSource="{Binding Movies}" SelectedItem="{Binding SelectedMovie}" Height="300" Background="#333" BorderBrush="Gray">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Tytuł" DisplayMemberBinding="{Binding Title}" Width="200"/>
                        <GridViewColumn Header="Gatunek" DisplayMemberBinding="{Binding Genre}" Width="150"/>
                        <GridViewColumn Header="Reżyser" DisplayMemberBinding="{Binding Director}" Width="150"/>
                        <GridViewColumn Header="Rok" DisplayMemberBinding="{Binding Year}" Width="80"/>
                        <GridViewColumn Header="Ocena" DisplayMemberBinding="{Binding Rating}" Width="80"/>
                    </GridView>
                </ListView.View>
            </ListView>
            <StackPanel Orientation="Horizontal" Margin="10">
                <Button Content="Dodaj Film" Command="{Binding AddMovieCommand}" Width="120" Background="#007ACC" Foreground="White" Margin="5"/>
                <Button Content="Usuń Film" Command="{Binding RemoveMovieCommand}" Width="120" Background="#E81123" Foreground="White" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>